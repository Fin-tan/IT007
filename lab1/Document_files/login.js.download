window.addEventListener('scroll', function() {
    let header = document.querySelector('#header');
    if (window.scrollY > 50) {
        header.classList.add('scrolled'); // Thêm class
    } else {
        header.classList.remove('scrolled'); // Xóa class nếu cuộn lên lại
    }
});
